# Security policy

This package has no network service, telemetry, credentials, or runtime asset
generator. Report a suspected dependency, build, or browser security issue
privately through the repository's GitHub Security Advisory form before opening
a public issue.

Only the latest pre-1.0 release is supported. Security fixes are documented in
the changelog and released from the same source as the public package code.
